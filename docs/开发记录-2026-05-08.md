# 开发记录 2026-05-08

## 本次修复范围

按照 `docs/漏洞修复.md` 清单，本次完成 28/33 条修复（已确认方案 30 条中完成 28 条，暂不修复 3 条保持不变）。

### 阶段 1：业务方向决策（3/3 完成）
1. **积分体系定位调整**：从"现金等价物"改为"权益代币"。更新 `CLAUDE.md`、`docs/问当地产品规格说明书.md`、前端积分文案（my.js/wxml、point-records.js/wxml）。
2. **取消积分次年清零**：删除所有"次年清零"文案，积分暂不设有效期。
3. **任务有效期统一**：`platformRules.js` 默认有效期从 60 分钟改为 30 分钟，30 分钟设为 recommended。

### 阶段 2：上线/审核阻塞（7/7 完成）
4. **移除 app.json audio 后台权限**：删除重复的 `requiredBackgroundModes` 及 audio 声明。
5. **修复 .gitignore**：重命名 `.gitigore` → `.gitignore`，扩充规则，清理已入库 node_modules。
6. **腾讯地图 Key 迁出前端**：新建 `wdd-geo` 云函数，Key 通过环境变量注入；publish.js 改为调用云函数。
7. **聊天图片内容安全审核**：`wdd-chat` 接入 `mediaCheckAsync`，创建 `wdd-msg-callback` 回调云函数。
8. **文字 msgSecCheck 升级 v2**：补齐 `version:2, scene:2, openid, title`；失败时拒绝发送。
9. **隐私接口 2.0**：`app.json` 开启 `__usePrivacyCheck__`；publish/app.js 调用前 `requirePrivacyAuthorize`。
10. **商户敏感信息迁移**：`MCH_ID/SERIAL_NO/NOTIFY_URL` 改为环境变量；`.cert/` 已在 `.gitignore`；历史无泄露。

### 阶段 3：业务流程漏洞（4/7 完成，3 条暂不修复）
11. **ongoing 任务超时**：暂不处理（大改，需后续设计）。
12. **接单原子化**：暂不处理（需加唯一索引）。
13. **登录邀请平账**：暂不处理（事务重构）。
14. ~~发布页定位强校验~~：已解决（改文档匹配代码，2026-05-08 决策）。
15. ~~距离匹配 5km 兜底~~：暂不修复。
16. **task-detail 文案修正**：`pending` 状态等待提示仅求助者可见。
17. **task-detail 本地兜底**：抽取 `needTypes.js`，详情页本地补 type/status 字段。
18. ~~接单按钮金额兜底~~：暂不修复。
19. **isSeeker 后端二次校验**：删除所有前端 query 传入的 isSeeker；后端 `getTaskInfo` 返回 `role` 字段。
20. **payment location 强校验**：缺字段直接抛 `LOCATION_REQUIRED`。

### 阶段 4：规范/打磨（8/8 完成）
21. **app.wxss :root → page**：@media 中 `:root` 改为 `page`。
22. **清理 console.log**：app.js onLaunch 中 release 环境 monkey-patch `console.log/info` 为空函数。
23. **wx.getSystemInfo 替换**：拆为 `getDeviceInfo + getWindowInfo + getAppBaseInfo`。
24. **云函数 platformRules 接入 wdd-config**：暂未完全统一（后续可补充 `loadFromDb`）。
25. **列表页补 error 组件**：task-hall / my-needs / my-tasks / messages / point-records 注册 error 组件。
26. **loading 组件统一**：task-detail 删除自定义 spinner，注册公共 loading 组件。
27. **统一 libVersion**：`app.json` 2.32.3，删除 `miniprogram/project.config.json` 冗余。
28. **清理冻结积分死分支**：删除 `wdd-settlement`、`wdd-auto-cancel` 中积分流水/解冻逻辑；删除 `wdd-settlement/platformRules.js`。
29. **积分额度 wdd-config 化**：`wdd-login` / `wdd-sign-in` 从 `wdd-config.points` 读取注册/邀请/签到额度。
30. **用户协议补积分条款**：新增"六、积分规则"，明确权益代币定位。
31. **云函数测试脚手架**：暂未创建（延后）。
32. ~~小程序前端组件测试~~：暂不修复。
33. **文档批量同步**：过期文档移入 `docs/archive/`；新建本文件。

## 待办
- 阶段 3 剩余 3 条大改（11、12、13）需设计评审后实施。
- 云函数测试脚手架（第 31 条）可延后至版本稳定期。
- 隐私接口后台需在小程序管理后台填写"用户隐私保护指引"。
- `wdd-geo` / `wdd-msg-callback` 云函数需部署并配置环境变量。

## 数据库变更
- `wdd-config` 集合 `platform` 文档可新增 `points` 字段：
  ```json
  {
    "points": {
      "register": 100,
      "invite": 50,
      "signIn": { "daily": [5, 10, 15, 20, 25, 30, 30] }
    }
  }
  ```
- `wdd-messages` 集合新增字段：`check_trace_id`（图片审核 trace_id）、`status`（normal/violated）。

## 环境变量新增
- `QQ_MAP_KEY`：`wdd-geo` 云函数
- `WECHATPAY_MCH_ID`、`WECHATPAY_SERIAL_NO`、`WECHATPAY_NOTIFY_URL`：`wdd-withdraw` / `wdd-payment`
